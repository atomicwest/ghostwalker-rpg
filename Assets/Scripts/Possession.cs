using UnityEngine;
using System.Collections;

public class Possession : MonoBehaviour {
	//trigger when a player approaches a possess-able object
	//AND presses X
	//this functions like the load new area,
	//but the player enters the next scene as the possessed NPC


	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		/*
		if (input.GetKeyDown (KeyCorde.X)) 
		{
			//play the possession animation
			//transfer player characteristics to npc object
			//destroy normal player character
		}
		*/

	
	}
}
